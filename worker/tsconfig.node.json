{
  // Tests and scripts run in Node.js (vitest, tsx), not in the Worker runtime.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["@cloudflare/workers-types", "node"]
  },
  "include": ["test/**/*.ts", "scripts/**/*.ts", "vitest.config.mts"]
}
